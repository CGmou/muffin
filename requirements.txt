# FastAPI >=0.118 accepts Starlette 1.x (older pins cap Starlette <0.42 and
# crash with "Router.__init__() got an unexpected keyword argument 'on_startup'"
# when a newer Starlette is present in a shared environment).
fastapi>=0.118
uvicorn[standard]>=0.29
requests>=2.31
